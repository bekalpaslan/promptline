node_modules/
src-tauri/target/
# Regenerated by tauri-build on every compile; capabilities/ is the source
src-tauri/gen/schemas/
dist/
design/dist/
.actionflows/

# Personal, not part of the product
PITCH-SCRIPT.md

# Aperant data directory
.auto-claude/

# Archify renders: the JSON sources are the truth, regenerate the rest
docs/architecture/**/*.html
docs/architecture/**/*.png
docs/architecture/**/*.visual-check.json
docs/architecture/**/*.receipt.json

# Copied in from docs/ by the Pages workflow; a local preview copy only
site/og.png

# Playwright output (npm run test:e2e)
test-results/
playwright-report/

# Copied in from docs/screenshots by the Pages workflow; local preview copies only
site/shots/
